name: Update Latest Repo

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

permissions:
  contents: write   # 🔥 IMPORTANT (fixes push error)

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4   # ✅ upgraded version

      - name: Get latest repo
        id: repo
        run: |
          repo=$(curl -s https://api.github.com/users/mrbeast-anirban/repos?sort=created | jq -r '.[0].name')
          echo "repo=$repo" >> $GITHUB_OUTPUT

      - name: Replace in README
        run: |
          sed -i "s/YOUR_LATEST_REPO/${{ steps.repo.outputs.repo }}/g" README.md

      - name: Commit changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add README.md
          git commit -m "Updated latest repo" || echo "No changes"
          git push